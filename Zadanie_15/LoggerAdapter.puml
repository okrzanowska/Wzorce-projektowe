@startuml LoggerAdapter
interface IOldLogger {
  + void LogMessage(string msg)
}

class ThirdPartyNewLogger {
  + void WriteLog(string text, int severity)
}

class LoggerAdapter implements IOldLogger {
  - ThirdPartyNewLogger newLogger
  + LoggerAdapter(ThirdPartyNewLogger newLogger)
  + void LogMessage(string msg)
}

LoggerAdapter --> ThirdPartyNewLogger : delegates to
@enduml